#include <stdio.h>
#include <stdlib.h>

    int contador = 100;

int main(void)
    {
    ContaCem();
    }

int ContaCem(){
    
   for (int i = 0; i <= contador; i++)
   {
       printf("%d\n",i);
   }
}